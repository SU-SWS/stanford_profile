uuid: 5a7b79c2-212b-4f8f-85fc-fb8d6eb0e4b2
langcode: en
status: true
dependencies:
  config:
    - config_pages.type.stanford_basic_site_settings
    - field.storage.config_pages.su_site_url
  module:
    - link
id: config_pages.stanford_basic_site_settings.su_site_url
field_name: su_site_url
entity_type: config_pages
bundle: stanford_basic_site_settings
label: 'Site URL'
description: 'Enter the canonical <em>base_url</em> for the site (no trailing slash). Note: You must have already configured the hostname in NetDB, and be accessing the site via that hostname.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  title: 0
  link_type: 16
field_type: link
