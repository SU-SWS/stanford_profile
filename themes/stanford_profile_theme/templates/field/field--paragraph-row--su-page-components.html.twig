{% set item_count = items|length %}
{% set possible_class = ['grid-cols-1', 'grid-cols-2', 'grid-cols-3'] %}
<div class="grid md:grid-cols-{{ item_count }}">
  {% for item in items %}
    <div{{ item.attributes }}>{{ item.content }}</div>
  {% endfor %}
</div>

