
<div{{ attributes }}>
  {{ title_prefix }}
  {{ include("jumpstart_ui:card", {
    header_tag: "h3",
    image: content.su_opp_image,
    super_headline: content.su_opp_sponsor,
    headline: node.label,
    headline_url: node.su_opp_source.get(0).getUrl().toString() ?? url,
    body: [
      content.su_opp_summary,
      content.su_opp_card_footer,
      content.su_opp_icon,
    ],
  }, with_context = false) }}
  {{ title_suffix }}
</div>
