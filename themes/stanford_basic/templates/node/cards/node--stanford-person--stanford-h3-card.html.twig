{% set headline %}
  <a href="{{ url }}">{{ label }}</a>
{% endset %}
<div{{ attributes }}>
  {{ include("jumpstart_ui:card", {
    header_tag: "h3",
    image: content["su_person_photo"],
    headline: headline,
    body: content["su_person_short_title"]
  }, with_context = false) }}
</div>
