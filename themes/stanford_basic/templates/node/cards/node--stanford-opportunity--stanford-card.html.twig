
<div{{ attributes }}>
  {{ title_prefix }}
  {{ include("jumpstart_ui:card", {
    header_tag: "h2",
    header_classes: "su-link--action",
    image: content.su_opp_image,
    super_headline: node.su_opp_sponsor.value,
    headline: node.label,
    headline_url: node.su_opp_source.get(0).getUrl().toString() ?: url,
    body: [
      content.su_opp_type,
      content.su_opp_summary,
    ],

  }, with_context = false) }}
  {{ title_suffix }}
</div>
