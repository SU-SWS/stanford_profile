
<div{{ attributes }}>
  {{ include("jumpstart_ui:card", {
    header_tag: "h2",
    super_headline: node.su_publication_citation.get(0).entity.getBundleEntity().label|default("Publication"),
    headline: node.label,
    headline_url: url,
    body: content.su_publication_topics
  }, with_context = false) }}
</div>
