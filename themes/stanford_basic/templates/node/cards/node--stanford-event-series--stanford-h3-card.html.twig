<div{{ attributes }}>
  {{ title_prefix }}
  {{ include("jumpstart_ui:card", {
    header_tag: "h3",
    headline: node.label,
    headline_url: url,
    body: node.su_event_series_subheadline.value
  }, with_context = false) }}
  {{ title_suffix }}
</div>
