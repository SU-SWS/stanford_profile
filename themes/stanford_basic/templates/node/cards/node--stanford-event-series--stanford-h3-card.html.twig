{% set headline %}
  <a href="{{ url }}">{{ label|render_clean }}</a>
{% endset %}
<div{{ attributes }}>
  {{ include("jumpstart_ui:card", {
    header_tag: "h3",
    headline: headline,
    body: node.su_event_series_subheadline.value
  }, with_context = false) }}
</div>
