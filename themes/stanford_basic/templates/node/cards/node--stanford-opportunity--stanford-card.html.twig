{% set headline %}
  <a href="{{ url }}">{{ label|render_plain }}</a>
{% endset %}
<div{{ attributes }}>
  {{ include("jumpstart_ui:card", {
    header_tag: "h2",
    image: content["su_opp_image"],
    super_headline: content["su_opp_sponsor"],
    headline: headline,
    body: [
      content["su_opp_type"],
      content["su_opp_summary"],
    ],

  }, with_context = false) }}
</div>
