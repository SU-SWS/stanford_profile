{% set code = node.su_course_subject.entity.name.value|default("") ~ " " ~ node.su_course_code.value|default("") %}

<div{{ attributes }}>
  {{ title_prefix }}
  TODO: Build Component
  {{ title_suffix }}
</div>
